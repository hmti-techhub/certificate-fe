import ThreeCircleLoading from "@/components/animation/ThreeCircleLoading";

const LoadingEvents = () => {
  return (
    <ThreeCircleLoading message="waiting events data..." className="pt-52" />
  );
};

export default LoadingEvents;
